const initialState = {}

export default function(state = initialState, action){
    switch(action.type){
        case "a":
          return initialState;
        default:
          return initialState;
    }
}
